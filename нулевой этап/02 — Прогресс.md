# Прогресс

## Учебный журнал

| Дата | Тема | Что сделал | Что понял | Следующий шаг |
|---|---|---|---|---|
| 2026-08-13 | Создание базы знаний | Подготовлена структура | Зафиксирован единый рабочий процесс | Оценить стартовый уровень |
| 2026-08-13 | Подготовка формата обучения | Согласованы чаты, теория, конспекты и повторения | Один урок продолжается до результата; прогресс хранится в базе | 14 августа начать урок 13 августа |
| 2026-08-13 | Linux: установка и терминал | Начат урок, состояние восстановлено из базы | Сначала нужно проверить среду виртуализации и исходный уровень Linux | Уточнить ОС, гипервизор и наличие Ubuntu Server VM |
| 2026-08-13 | Linux: подготовка стенда | Определены Mac M2, Parallels и установка VM с нуля | На Apple Silicon нужен ARM64-образ; Shared Network в Parallels соответствует NAT | Скачать Ubuntu Server 24.04.4 LTS ARM64 и создать VM |
| 2026-08-14 | Linux: установка Ubuntu Server | Создана ARM64 VM, выбраны язык и клавиатура, открыт выбор типа установки | Обычный Ubuntu Server подходит для интерактивного учебного стенда | Выбрать обычную установку и проверить DHCP на сетевом экране |
| 2026-08-14 | Linux: сеть установщика | Интерфейс `enp0s5` получил `10.211.55.6/24` по DHCPv4 | Shared Network/NAT в Parallels работает; ручная адресация не нужна | Подтвердить сеть, пропустить proxy и проверить зеркало Ubuntu |
| 2026-08-14 | Linux: диагностика DNS | Проверка `ports.ubuntu.com` завершилась временной ошибкой разрешения имени | Получение DHCP-адреса само по себе ещё не доказывает работу DNS и доступа к репозиторию | Повторить проверку один раз; при повторе диагностировать Shared Network/DNS |
| 2026-08-14 | Linux: диагностика зеркала | Повторная попытка разрешила домен, но HTTP вернул `503` и сброс соединения | DNS уже работает; код HTTP и IP-адрес позволяют локализовать проблему дальше DNS | Проверить то же официальное ARM64-зеркало по HTTPS |
| 2026-08-14 | Linux: применение настройки | После ввода HTTPS журнал проверки всё ещё показал HTTP и порт 80 | Отображённое в поле значение не обязательно применено; фактический протокол проверяется по журналу | Подтвердить HTTPS URL клавишей Enter и проверить новый журнал |
| 2026-08-14 | Linux: решение по установке | Mirror не заработал стабильно ни с VPN, ни без него | Базовую ОС можно установить с ISO, а сеть и APT надёжнее диагностировать после загрузки | Продолжить без сетевых обновлений и проверить разметку диска |
| 2026-08-14 | Linux: разметка VM | Проверен единственный виртуальный диск 32 GB; выбраны весь диск и LVM без LUKS | `Use an entire disk` затрагивает выбранный виртуальный диск, а финальную схему нужно проверить до записи | Открыть итоговую схему разделов без подтверждения записи |
| 2026-08-14 | Linux: проверка схемы диска | Проверены EFI 1.049 GB, `/boot` 2 GB, LVM VG 28.95 GB и корневой LV 14.472 GB | Свободные 14.472 GB находятся внутри LVM и доступны для будущего расширения | Подтвердить форматирование виртуального диска и создать профиль |
| 2026-08-14 | Linux: профиль системы | Созданы обычный пользователь `egor` и hostname `ubuntu-study`; пароль не сохранён в базе | SSH-службу не нужно включать до урока по её осознанной настройке | Пропустить OpenSSH и snaps, завершить установку |
| 2026-08-14 | Linux: первая загрузка | Ubuntu Server 24.04.4 LTS загрузилась, `cloud-init` завершился успешно | VM уже стартует с виртуального диска; теперь нужно доказать вход обычного пользователя | Войти как `egor` и выполнить `whoami`, `hostname`, `pwd` |
| 2026-08-14 | Linux: вход пользователя | Подтверждены `egor`, `ubuntu-study`, `/home/egor` | Приглашение shell и команды позволяют проверить пользователя, хост и текущий каталог | Проверить адрес, маршрут и DNS без изменения сети |
| 2026-08-14 | Linux: IP и маршрут | Подтверждены `enp0s5` с `10.211.55.6/24` и default via `10.211.55.1`; в `getent ahosts` пропущен hostname | Успешный запуск команды не гарантирует ответ на нужный вопрос; аргументы важны | Повторить `getent ahosts ports.ubuntu.com` |
| 2026-08-14 | Linux: внешний DNS | Точная команда `getent ahosts ports.ubuntu.com` не вернула вывода | IP и маршрут могут работать при неисправном DNS; уровни нужно проверять отдельно | Посмотреть `resolvectl status` и `/etc/resolv.conf` |
| 2026-08-14 | Linux: путь DNS-запроса | Найдены stub `127.0.0.53` и uplink DNS Parallels `10.211.55.1`; опечатка `resolve.conf` исправлена на `resolv.conf` | Приложение обращается к локальному resolver, который пересылает запрос uplink-серверу; точность пути важна | Сравнить `resolvectl query` для обычного домена и Ubuntu mirror |
| 2026-08-14 | Linux: DNS-forwarder | Запросы `example.com` и `ports.ubuntu.com` зависли и были прерваны `Ctrl+C` | Сбой не зависит от конкретного домена; нужно проверить шлюз и внешний IP ниже уровня DNS | Выполнить ping до `10.211.55.1` и `1.1.1.1` |
| 2026-08-14 | Linux: гипотеза VPN | Симптом совпал с ранее наблюдавшимся конфликтом Happ и DNS-прокси Parallels; `1.1.1.1` не отвечает на ping | ICMP может блокироваться при рабочем TCP; влияние VPN нужно проверять A/B-тестом | Отключить Happ и повторить `resolvectl query example.com` |
| 2026-08-14 | Linux: причина DNS-сбоя | Без Happ `resolvectl query example.com` успешно ответил за 70,8 мс | A/B-тест отделил исправную Ubuntu/Parallels-сеть от конфликта Happ с DNS-forwarder | Без VPN проверить Ubuntu mirror и `sudo apt update` |
| 2026-08-14 | Linux: проверка APT | DNS разрешил `ports.ubuntu.com`, но HTTP mirror вернул `503` на порту 80; команда остановлена `Ctrl+C` | DNS и HTTP — разные уровни: имя уже разрешается, сбой теперь в доступе к сервису зеркала | Найти активную запись APT и перевести официальный mirror на HTTPS |
| 2026-08-14 | Linux: источник APT | Найден активный `/etc/apt/sources.list.d/ubuntu.sources`; `ubuntu.sources.curtin.orig` не является активным источником | Перед изменением конфигурации нужно определить точный активный файл и сохранить возможность отката | Создать копию, заменить HTTP на HTTPS и повторить `apt update` |
| 2026-08-14 | Linux: TLS зеркала | HTTP URI заменены на HTTPS, но APT получил `Could not handshake: Error in the pull function` на порту 443 | DNS/TCP-доступ и TLS handshake — отдельные этапы; версия 24.04.4 соответствует `noble` | Проверить тот же URL через IPv4 `curl` вне APT |
| 2026-08-14 | Linux: подтверждение TLS-сброса | Три `curl -4` к `ports.ubuntu.com` завершились `Connection reset by peer` | Повторяемый сброс вне APT исключает версию пакетного менеджера и указывает на маршрут или endpoint | Проверить официальный `us.ports.ubuntu.com` |
| 2026-08-14 | Linux: сравнение endpoint | `us.ports.ubuntu.com` также дважды сбросил TLS | Проблема не ограничена одним сервером; без VPN вероятен сбой или фильтрация маршрута Ubuntu Ports | Проверить VPN с временным прямым DNS на интерфейсе VM |
| 2026-08-14 | Linux: рабочая сеть | С VPN и прямыми DNS `1.1.1.1/1.0.0.1` домен разрешился, HTTPS вернул `200 OK` | Для стенда нужны одновременно VPN для маршрута и обход DNS-прокси Parallels; причины независимы | Выполнить `sudo apt update` в рабочей комбинации |
| 2026-08-14 | Linux: APT работает | `sudo apt update` скачал 14,6 MB индексов; доступно 174 обновления | Наблюдаемые `503/reset` без VPN совместимы с фильтрацией/маршрутом, но не доказывают whitelist | Посмотреть netplan и закрепить прямой DNS, не меняя DHCP |
| 2026-08-14 | Linux: Netplan | Найден `50-cloud-init.yaml` с `enp0s5` и `dhcp4: true`; секретов нет | Генерируемый cloud-init-файл лучше не переписывать: локальное переопределение хранится отдельно и сначала валидируется | Создать `99-study-dns.yaml` и выполнить `netplan generate` |
| 2026-08-14 | Linux: права Netplan | YAML прошёл синтаксическую обработку, но Netplan предупредил о слишком открытых правах `99-study-dns.yaml` | Конфигурационные файлы сети должны быть доступны только root; предупреждение нужно устранить до применения | Установить mode 600 и повторить `netplan generate` |
| 2026-08-14 | Linux: валидация Netplan | Установлены права `600`; `netplan generate` завершился чисто | Пустой успешный вывод означает отсутствие найденных ошибок; применять сеть безопаснее через режим с откатом | Выполнить `netplan try` и проверить DNS |
| 2026-08-14 | Linux: постоянный DNS | `netplan try` применён; `enp0s5` использует `1.1.1.1/1.0.0.1`, DNS-запрос успешен | DHCP можно сохранить для адреса/маршрута, одновременно отказавшись только от DNS, выданного DHCP | Установить 174 обновления и проверить конфигурацию после reboot |
| 2026-08-14 | Linux: обновление системы | `apt upgrade` завершён, установлено ядро `6.8.0-137-generic`; текущее ядро ещё `6.8.0-100-generic` | Установка ядра не переключает уже запущенное ядро; для активации нужен reboot | Перезагрузить и проверить `uname -r` и DNS |
| 2026-08-14 | Linux: проверка после reboot | Подтверждены ядро `6.8.0-137-generic`, постоянный прямой DNS и разрешение Ubuntu Ports | Конфигурацию нужно проверять после перезагрузки; подсказка `command not found` не является причиной автоматически ставить пакет | Самостоятельно выполнить безопасный цикл файловых операций в `~/study` |
| 2026-08-14 | Linux: корректировка занятия | Пользователь запросил теорию перед файловой практикой | Проект должен давать необходимую теорию сам; практика следует после небольшого объяснения и проверки | Разобрать пути и команды навигации |
| 2026-08-14 | Linux: навигация | Пользователь верно объяснил абсолютные/относительные пути, `cd ..`, `pwd` и проверки перед удалением | `pwd` подтверждает место, `ls` — объекты в нём; из `~/study` родитель — `/home/egor` | Разобрать создание, чтение, копирование, перемещение, поиск и удаление файлов |
| 2026-08-14 | Linux: файловые команды | Верно объяснены `>`/`>>`, результаты `cp`/`mv`, работа `file` и безопасное `rm -i` | Теория понята на уровне прогноза результата; теперь нужна самостоятельная практика | Выполнить ограниченный цикл в `~/study/linux` без `sudo` |
| 2026-08-14 | Linux: файловая практика | Создана структура `~/study`; оригинал записан/прочитан, копия создана, переименована и удалена через `rm -i`; опечатки исправлены | Точные имена критичны; итог верный, но `find`/`file` применены к оригиналу, а ранний `rm` был без подтверждения | Объяснить ошибки и повторить поиск/тип именно копии перед безопасным удалением |
| 2026-08-14 | Linux: корректирующая практика | Причины ошибок объяснены; `notes-check.txt` найден, проверен через `file` и безопасно удалён после `pwd`/`ls` | Ошибка становится полезным результатом, когда причина объяснена и правильное действие повторено самостоятельно | Разобрать основные каталоги Linux и команды справки |
| 2026-08-14 | Linux: структура ФС | Верно объяснены `/etc`, `/var`, `/home`, `/usr`, `/tmp` и журналы; выявлен пробел по `/proc` | `/proc` показывает псевдофайлы, содержимое которых формирует ядро при чтении, а не обычные данные на диске | Прочитать `/proc/version` и освоить `which`, `man`, `history` |
| 2026-08-14 | Linux: встроенная справка | `which cat` и `history 10` выполнены; в `man pwd` описание принято за опцию | `cat` только читает `/proc`; `man` нужно читать по секциям, отличая общее описание от списка OPTIONS | Назвать опцию `pwd` и объяснить отличие `man` от `--help` |
| 2026-08-14 | Linux: уточнения закрыты | Пользователь верно объяснил роль ядра и `cat`, выбрал `pwd -P` и назначение `man` | Виртуальный файл предоставляет ядро; `--help` — краткая памятка, `man` — полное руководство | Сформулировать итог урока своими словами для конспекта |
| 2026-08-14 | Linux: черновик конспекта | Написан собственный итог по каталогам, путям, командам и безопасности | Перед сохранением нужно отличить `/home` от конкретного home, относительный путь от «локального», `/dev` от `/proc` и добавить `/tmp` | Переформулировать четыре строки своими словами |
| 2026-08-14 | Linux: уточнение конспекта | Исправлены `/home`, `/tmp` и пути; `/dev` назван возможным аналогом драйверов | Запись `/dev` — интерфейс/device node, драйвер — код в ядре или модуле; их нельзя отождествлять | Объяснить различие одним предложением |
| 2026-08-14 | Linux: модель устройства | Пользователь отделил `/dev` от драйвера, но связал драйвер только с сигналами и током | Драйвер переводит операции ОС в команды устройства/протокола; `/dev` предоставляет специальный файловый интерфейс | Дать одну точную формулировку своими словами |
| 2026-08-14 | Linux: конспект | Пользователь точно объяснил `/dev` и драйвер; собственные формулировки оформлены в конспект | Конспект фиксирует подтверждённое понимание, практический пример, ошибки и безопасные ограничения | Пройти финальную мини-проверку урока |
| 2026-08-14 | Linux: финальная проверка | Верно объяснены каталоги, `rm -i`, `/proc` и `/dev`; пропущен `file`, а проверка `ls` не была направлена на удаляемый путь | Проверки безопасности должны подтверждать точную цель, а не просто выполняться формально | Исправить две последовательности команд |
| 2026-08-15 | Linux: финальная коррекция | Последовательность безопасного удаления исправлена; `find` и `file` записаны одной командой, для `file` указан путь без каталога `linux` | Две самостоятельные команды нельзя разделять только пробелом; следующая команда должна использовать фактический путь найденного файла | Исправить только последовательность поиска и определения типа |
| 2026-08-15 | Linux: точность пути | `find` и `file` разделены правильно, но в аргументе `file` снова пропущен каталог `linux` | Вывод поиска нужно не только прочитать, но и точно перенести в следующую команду | Исправить только аргумент команды `file` |
| 2026-08-15 | Linux: урок завершён | Команда `file ~/study/linux/notes.txt` использовала точный путь из результата поиска; все критерии урока подтверждены | Поиск и последующее действие — отдельные операции; найденный путь нужно переносить без изменений | На следующем занятии выполнить повторение +1 и начать пользователей и права |

## Еженедельный обзор

### Неделя: YYYY-MM-DD — YYYY-MM-DD

- Главный результат:
- Изученные темы:
- Выполненные лабораторные:
- Что пока не понимаю:
- Ошибка недели и чему она научила:
- План следующей недели:

## Интервальные повторения

Для каждой важной темы отмечаем повторение после первого изучения.

| Тема | Изучено | +1 день | +7 дней | +30 дней | Уверенность 1–5 |
|---|---|---|---|---|---|
| Linux: файловая система и терминал | 2026-08-14 | 2026-08-15 — запланировано | 2026-08-21 — запланировано | 2026-09-13 — запланировано | 4 |

## Критерий освоения темы

- Могу объяснить без подсказки.
- Могу привести безопасный практический пример.
- Понимаю типичные ошибки и способы защиты.
- Могу связать тему минимум с двумя другими заметками.
